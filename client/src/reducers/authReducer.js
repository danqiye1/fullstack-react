/** authReducer
 * Responsible for keeping track if user is logged in.
 */
export default function (state = {}, action) {
    console.log(action);
    switch (action.type) {
        default:
            return state;
    }
}